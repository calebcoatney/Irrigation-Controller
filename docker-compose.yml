services:
  backend:
    build: ./backend
    devices:
      - /dev/irrigation_relay:/dev/irrigation_relay
    restart: unless-stopped

  frontend:
    build:
      context: .
      dockerfile: frontend/Dockerfile
    ports:
      - "80:80"
    depends_on:
      - backend
    restart: unless-stopped
